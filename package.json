{
  "name": "projektwahl-lit",
  "version": "0.0.1",
  "license": "AGPL-3.0-or-later",
  "author": {
    "name": "Moritz Hedtke",
    "email": "Moritz.Hedtke@t-online.de"
  },
  "bugs": {
    "url": "https://github.com/mohe2015/projektwahl-lit/issues"
  },
  "homepage": "https://github.com/mohe2015/projektwahl-lit",
  "repository": {
    "type": "git",
    "url": "https://github.com/mohe2015/projektwahl-lit.git"
  },
  "description": "Software to manage choosing projects and automatically assigning people to projects.",
  "type": "module",
  "scripts": {
    "format": "docker build -t getting-started . && docker run --mount type=bind,source=\"$(pwd)\"/tests,target=/opt/projektwahl-lit/tests --mount type=bind,source=\"$(pwd)\"/src,target=/opt/projektwahl-lit/src -it getting-started npm run format",
    "format:check": "prettier --ignore-path .prettierignore --check .",
    "test": "docker build -t getting-started . && docker run --mount type=bind,source=\"$(pwd)\"/tests/e2e/new-tests.ts,target=/opt/projektwahl-lit/tests/e2e/new-tests.ts --net=host -e BASE_URL -e CI=1 -it getting-started npm run test",
    "lint": "docker build -t getting-started . && docker run --mount type=bind,source=\"$(pwd)\"/tests,target=/opt/projektwahl-lit/tests --mount type=bind,source=\"$(pwd)\"/src,target=/opt/projektwahl-lit/src -it getting-started npm run lint",
    "lint:fix": "docker build -t getting-started . && docker run --mount type=bind,source=\"$(pwd)\"/tests,target=/opt/projektwahl-lit/tests --mount type=bind,source=\"$(pwd)\"/src,target=/opt/projektwahl-lit/src -it getting-started npm run lint:fix",
    "evaluate": "node --experimental-loader ./src/loader.js --enable-source-maps ./src/server/routes/evaluate/index.js",
    "setup": "node --experimental-loader ./src/loader.js --enable-source-maps ./src/server/setup.js",
    "server": "node --experimental-loader ./src/loader.js --enable-source-maps --experimental-import-meta-resolve ./src/server/index.js",
    "localize-extract": "mkdir -p src/client/generated && docker build -t getting-started . && docker run --mount type=bind,source=\"$(pwd)\"/xliff,target=/opt/projektwahl-lit/xliff -it getting-started npm run localize-extract",
    "localize-build": "mkdir -p src/client/generated && docker build -t getting-started . && docker run --mount type=bind,source=\"$(pwd)\"/src/client/generated,target=/opt/projektwahl-lit/src/client/generated -it getting-started npm run localize-build",
    "typecheck": "tsc",
    "lit-analyze": "lit-analyzer --strict --rules.no-unknown-attribute off src",
    "build": "node build.js"
  },
  "dependencies": {
    "@dev.mohe/argon2": "^0.29.4",
    "@dev.mohe/task": "^1.1.1",
    "@lit-labs/motion": "^1.0.2",
    "@lit/localize": "^0.11.2",
    "bootstrap": "^5.1.3",
    "cookie": "^0.4.2",
    "csv-parse": "^5.0.4",
    "js-cookie": "^3.0.1",
    "lit": "^2.2.1",
    "openid-client": "^5.1.4",
    "postgres": "porsager/postgres",
    "zod": "^3.14.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "esbuild-linux-64": "^0.14.27"
  },
  "optionalDependencies": {
    "@types/cookie": "^0.4.1",
    "@types/js-cookie": "^3.0.1",
    "@types/node": "^17.0.22",
    "typescript": "^4.6.2"
  }
}
